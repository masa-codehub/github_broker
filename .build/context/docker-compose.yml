services:
  service-name:
    build:
      context: ./${BROKER_NAME}/.build
      dockerfile: Dockerfile
    image: ${BROKER_NAME}_image
    volumes:
      - ./${BROKER_NAME}:${CONTAINER_VOLUME:-/app}
    working_dir: ${CONTAINER_VOLUME:-/app}
    ports:
      - "${BROKER_PORT:-8080}:${BROKER_PORT:-8080}"
    environment:
      - AGENT_ID=${AGENT_ID:-agent_id}
      - AGENT_MESSAGE_DIR=${AGENT_MESSAGE_DIR:-/app/messages}
      - BROKER_PORT=${BROKER_PORT:-8080}
      - GITHUB_REPOSITORY=${GITHUB_REPOSITORY:-your_github_repo}
      - GH_TOKEN=${GH_TOKEN:-my_github_token}
      - GOOGLE_CLOUD_PROJECT=${GOOGLE_CLOUD_PROJECT:-your_gcp_project}
      - GOOGLE_CLOUD_LOCATION=${GOOGLE_CLOUD_LOCATION:-us-central1}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    # command: ["bash", "run.sh"]
    stdin_open: true
    tty: true

  redis:
    image: redis:latest
    hostname: redis
    ports:
      - "6379:6379"